services:
  # The Database Engine
  db:
    image: pgvector/pgvector:0.8.2-pg18-trixie
    container_name: pg_db
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: safe_gateway
    volumes:
      - ./postgres_data:/var/lib/postgresql
    networks:
      - pg_network

  # The Management UI (Dashboard)
  pgadmin:
    image: dpage/pgadmin4:snapshot 
    container_name: pgadmin_ui
    restart: always
    ports:
      - "5050:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin
    depends_on:
      - db
    networks:
      - pg_network

# Persistence and Networking
networks:
  pg_network:

volumes:
  pgdata: